<%= title %>

<%= description %>
<% if (reviewers) { %>===CODE REVIEW===<% }); %>
<% _.forEach(reviewers, function(reviewer) { %> - [ ] <%- reviewer %>
<% }); %>

<% if (testers) { %>===Testing===<% }); %>
<% _.forEach(testers, function(reviewer) { %> - [ ] <%- reviewer %>
<% }); %>

<% if (additionalRequirements) { %>===Additional Requirements===<% }); %>
<% _.forEach(additionalRequirements, function(additionalRequirement) { %> - [ ] <%- additionalRequirement %>
<% }); %>
